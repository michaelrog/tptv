@tailwind base;
@tailwind utilities;

html {
	scroll-behavior: smooth;
}

html, body {
	margin: 0;
	padding: 0;
}

* {
	@apply antialiased;
}

body {
	display: relative;
}

iframe#player {
	@apply absolute w-full h-full inset-0 z-10;
}
